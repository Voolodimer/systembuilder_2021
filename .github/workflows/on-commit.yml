name: Commit-Action

on:
  push:
    branches:
<<<<<<< HEAD
    - feat/chore-update-status

jobs:
  updater:
    if: "!startsWith(github.event.head_commit.message, 'chore: upload status')"
    runs-on: ubuntu-latest
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      DEV_BOT_TOKEN: ${{ secrets.DEV_BOT_TOKEN }}
    steps:
    - name: Git_clone_repo
      uses: actions/checkout@v2
      with:
        repository: Voolodimer/systembuilder_2021
        token: ${{ secrets.DEV_BOT_TOKEN }}

    - name: run_builder
      run: python ./src/builder.py

    - name: Set_commit
      run : |
        pwd
        ls
        git config --global user.name = "dev-bot0"
        git config --global user.email = aka1306@gmail.com
        git add status.json
        git commit -qm "chore: update status"

    - name: Push commit
      uses: ad-m/github-push-action@master
      with:
        github_token: ${{ env.DEV_BOT_TOKEN }}
        repository: antonkurenkov/systembuilder_2021
        branch: develop
        force: true
=======
      - ref/builder
jobs:
  build:
    name: Single Job
    if: "!startsWith(github.event.head_commit.message, 'chore')"
    runs-on: ubuntu-latest
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
    
    steps:

      - name: Setup Python 3
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Cloning the repo as dev-bot
        uses: actions/checkout@v2 
        with:
          repository: antonkurenkov/systembuilder_2021
          token: ${{ secrets.DEV_BOT_TOKEN }}
          path: repo

      - name: Installing dependencies
        run: |
          cd repo
          sudo apt-get -y update
          python -m pip install --upgrade pip setuptools wheel testresources
          python -m pip install -r requirements.txt

      - name: Building projects
        run: |
          cd repo
          python src/builder.py

      - name: Committing updated status map
        run: |
          cd repo
          git config --local user.email "aka1306@gmail.com"
          git config --local user.name "dev-bot0"
          git add status.json
          git commit -am 'chore: update status'

      - name: Pushing updated status map
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.DEV_BOT_TOKEN }}
          repository: antonkurenkov/systembuilder_2021
          branch: ${{ github.ref }}
          force: true
          directory: repo

      - name: Sending message to telegram chat
        run: |
          cd repo
          python chore/notifier.py
>>>>>>> 8d0d804dc4c418acf6df42712041ce66105b0124
